package class24;

import java.util.ArrayList;

public class E1ArrayList {
    public static void main(String[] args) {
        /*for int datatype we have integer
          for byte we have Byte
          for short we have Short
          for long we have Long
          for float we have Float
          for double we have Double
          for boolean we have Boolean
          for char we have Character
         */
        ArrayList<Integer>arrayList=new ArrayList();
        /*Integer integer=new Integer(10);
        Integer integer1=new Integer(20);
         */
        Integer integer=10;
        Integer integer1=20;
        String str="no";

        System.out.println(integer1+integer);
    }

}
