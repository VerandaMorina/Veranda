Super keyword:

       By default javas preference for variables:
             1)Local
             2)Instance variables of the same class
             3)Instance variables of parent class.


If we want to modify this default preference we can use "this" ans "super" keyword
with the help of super keyword we can acces the constructor of the same class and parent class