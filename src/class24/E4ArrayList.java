package class24;

public class E4ArrayList {
    public static void main(String[] args) {

        Integer integer=new Integer(10);//convert the primitive to a wraper type that is called boxing
        Integer integer1=10;//convert the primitives to a wraper type this is called autoboxing //this we use
        int num=integer1.intValue();//unboxing
        int num2=integer1;//auto unboxing //this we use
        System.out.println(integer1);
    }
}
